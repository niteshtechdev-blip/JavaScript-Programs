function reverseNum(num){
    let rev=0;
    let cpy=num
    while (num>0) {
        r
        num=num/10
    }
    return `Reverse of ${cpy}: ${rev}`;
}

console.log(reverseNum(12));
//190